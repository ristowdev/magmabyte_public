import React from 'react';
import { CircularProgresAnimation } from './styles';

interface ICircularLoaderProps {
  
}

export default function CircularLoader(props: ICircularLoaderProps) {
    const {

    } = props;

    return (
        <>
            <CircularProgresAnimation />
        </>
    );
}